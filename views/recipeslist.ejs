<!DOCTYPE html>
<html>

  <head>
    <title>Recipies</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>

  <body>

    <h3>Recipies</h3>
    <ul>
        <% recipeslist.forEach(function(recipe) { %>
            <li> <a href="viewRecipe?recipe=<%= recipe.title %>"> <%= recipe.title %> </a> </li>
        <% }); %>
    </ul>
  </body>

</html>
